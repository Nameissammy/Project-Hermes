[project]
name = "project_hermes_frontend"
version = "0.1.0"
description = "Frontend for Project Hermes travel planning system"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "streamlit>=1.37.1",
    "requests>=2.31.0",
    "python-dotenv>=1.1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/project_hermes_frontend"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
